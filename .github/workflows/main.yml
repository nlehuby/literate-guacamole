name: rss

on:
  schedule:
  - cron: '12 * * * *'

jobs:
  new-traces:
    runs-on: ubuntu-latest
    steps:
    - uses: guilhem/rss-issues-action@0.1.0
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        feed: "https://www.openstreetmap.org/traces/rss"
        prefix: "[Trace]"
        dry-run: "true"
        lastTime: "336h"
        labels: "à qualifier"
